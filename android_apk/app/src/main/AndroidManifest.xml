<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.BusWidget"
        android:supportsRtl="true"
        android:name=".App">

        <!-- Main stops list screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Arrivals polling screen -->
        <activity
            android:name=".ArrivalsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Add stop screen -->
        <activity
            android:name=".AddStopActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Map stop picker -->
        <activity
            android:name=".MapPickerActivity"
            android:exported="false"/>

        <!-- About screen -->
        <activity
            android:name=".AboutActivity"
            android:exported="false"/>

        <!-- Invisible router for the Quick Settings tile -->
        <activity
            android:name=".NearestStopActivity"
            android:exported="false"
            android:theme="@style/Theme.BusWidget.Translucent"
            android:excludeFromRecents="true"
            android:taskAffinity=""/>

        <!-- Quick Settings tile: open the nearest stop's arrivals -->
        <service
            android:name=".BusTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_bus"
            android:label="Ближайший автобус"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

        <!-- Widget configuration screen -->
        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.BusWidget"/>

        <!-- Base receiver for the manual widget's START/STOP button broadcasts.
             Registered (so explicit intents reach it) but has no appwidget
             meta-data, so it is NOT offered as a separate widget in the picker. -->
        <receiver
            android:name=".widget.BusWidgetProvider"
            android:exported="false"/>

        <!-- Wide 3×1 widget (manual stop selection) -->
        <receiver
            android:name=".widget.BusWidgetProviderWide"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bus_widget_info_wide"/>
        </receiver>

        <!-- Auto 3×1 widget (finds nearest stop by geolocation) -->
        <receiver
            android:name=".widget.BusWidgetProviderAuto"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bus_widget_info_auto"/>
        </receiver>

        <!-- Experimental 4×2 widget: static map snippet + nearest 3 buses -->
        <receiver
            android:name=".widget.MapWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bus_widget_info_map"/>
        </receiver>

        <!-- Background polling service -->
        <service
            android:name=".widget.PollService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Auto-widget session service (resolves nearest stop by geolocation) -->
        <service
            android:name=".widget.AutoPollService"
            android:exported="false"
            android:foregroundServiceType="location|dataSync"/>

        <!-- Single-bus watch service ("напомнить, когда автобус будет в N мин") -->
        <service
            android:name=".widget.WatchService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

    </application>
</manifest>
