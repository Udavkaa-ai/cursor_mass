<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.BusWidget"
        android:supportsRtl="true"
        android:name=".App">

        <meta-data
            android:name="com.yandex.android.maps.MAPKIT_API_KEY"
            android:value="bf1da5ff-0a6b-4056-a5ea-6435b6bb85f8"/>

        <!-- Main stops list screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Arrivals polling screen -->
        <activity
            android:name=".ArrivalsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Add stop screen -->
        <activity
            android:name=".AddStopActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Map stop picker -->
        <activity
            android:name=".MapPickerActivity"
            android:exported="false"/>

        <!-- Widget configuration screen -->
        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.BusWidget"/>

        <!-- Home screen widget provider -->
        <receiver
            android:name=".widget.BusWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bus_widget_info"/>
        </receiver>

        <!-- Background polling service -->
        <service
            android:name=".widget.PollService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

    </application>
</manifest>
